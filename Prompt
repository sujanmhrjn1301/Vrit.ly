i need to build the following thing

Tech Stack:

Python Django (Backend)

Lets use Django's template for the frontend



Task Details:

Create a URL shortener web application using Python with the Django framework.



1. A Landing Page similar to TinyURL or BitURL where users can convert long URLs into shortURLs and in the backend the shortURL should point to the original URL.

2. In the landing page, in the nav bar top right, we need Log In, and  Create Account



Below features are only for the authenticated/logged in users only.

3. Users should be able to register, log in, and log out.

4. Authenticated/Logged In users Only: They can create, view, edit, delete their short URLs.

5. Authenticated./Logged in user only: Display additional information, such as creation date and usage statistics.

6. Implement a unique, short key generation algorithm for each URL (e.g., base62

encoding).

7. Implement basic analytics for each short URL (e.g., click count).

Display the number of clicks for each short URL.

8. Allow users to customize the short part of the URL.

9. Ensure that custom short URLs are unique and available.

10. Implement the option for users to set an expiration time for short URLs.

11. QR Code Generation: Provide the ability to generate QR codes for short URLs. Not the QRCode for the Long URLs, but for the ShortURLs Remember this.